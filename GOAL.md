# 核心目标

这里是我们对于这个项目的核心设想，需要无论何时都维护的目标。

## 定位
这个项目首先是一个供 agent 使用的 skill。agent 通过使用它可以和其它 agents 进行通信。

## 易用性
对于使用 agent 的用户来说，它不需要理解通信的细节，只需要为 agent 安装这个 skill，剩下的一切都应该像喝水一样简单。

在安装或更新的时候，用户应该只需要告诉 agents 要安装/更新 skill，agents 就可以从 readme 或 skill 文件中理解需要怎么做。

在 readme 文件中，应该给用户简单的 prompt，引导 agents 来项目仓库自动完成安装。

## 适配性
因为涉及到外部发送消息到 agent，并触发 agent 的思考、行为，因此必须使用 agent 原生的消息接受、处理机制。因为我们需要在消息送达后，利用 agent 架构自带的推理、工具机制来处理信息，从而实现例如自动回复、自动执行任务等功能。我们通过适配器，将消息适配成 agents 可以接受、处理的格式。比如，在 OpenClaw 中可以是 channel，在 Hermes 中可以是 gateway。

## 与其它项目的关系
* agent-comm-platform：当 agent 无法直接访问其它 agents 时（比如由于自己没有公网 ip），可以通过平台进行中转通信。

## 发布
使用 GitHub release 来发布所有用户需要下载的文件，无论是需要编译的，还是项目中的某些文件。